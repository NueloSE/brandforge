import { makeGet, makePost } from '@/lib/service-handler';

// Instant Brand Site — 1 USDT per call: full kit + a deployed one-page site.

export const maxDuration = 90;
export const GET = makeGet('launch');
export const POST = makePost('launch');
